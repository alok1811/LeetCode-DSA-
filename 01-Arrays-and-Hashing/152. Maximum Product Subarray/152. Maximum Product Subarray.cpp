        long ans = nums[0];        for(int i = 1; i < n; i++){            long value0 = minEnd * nums[i];            long value1 = maxEnd * nums[i];            long value2 = nums[i];            minEnd = min(value0, min(value1, value2));        long maxEnd = nums[0];        long minEnd = nums[0];            maxEnd = max(value0, max(value1, value2));            ans = max(ans, max(minEnd, maxEnd));        }        return ans;    }};        int n = nums.size();    int maxProduct(vector<int>& nums) {class Solution {public: